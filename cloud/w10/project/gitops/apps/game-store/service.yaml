apiVersion: v1
kind: Service
metadata:
  name: game-store
  labels:
    app.kubernetes.io/name: game-store
    app.kubernetes.io/part-of: w10-project
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: game-store
  ports:
    - name: http
      port: 80
      targetPort: http
