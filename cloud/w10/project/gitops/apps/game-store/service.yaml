apiVersion: v1
kind: Service
metadata:
  name: game-store
  namespace: demo
spec:
  type: NodePort
  selector:
    app: game-store
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30081 